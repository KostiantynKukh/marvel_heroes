import React from 'react';
import './header.scss'
function Header(props) {
    return (
        <header className="header">
            <h1 className="title">
                marvel heroes
            </h1>
        </header>

    );
}

export default Header;